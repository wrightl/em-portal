# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: em-portal
services:  
  app:
    language: dotnet
    project: ./src/host/EmPortal.AppHost/EmPortal.AppHost.csproj
    host: containerapp
